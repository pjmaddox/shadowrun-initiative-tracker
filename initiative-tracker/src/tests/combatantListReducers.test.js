import { combatantReducer } from "../stores/reducers/combatantListReducers.js";

describe("initiativeApp", () => {
    it("should return an empty llist of combatants when no current state given", () => {
        
    });
});